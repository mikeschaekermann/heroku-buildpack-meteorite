#!/usr/bin/env bash
# bin/release <build-dir>

cat <<EOF
---
addons:
  - mongohq:sandbox
default_process_types:
  web: $MRT_APP_DIR/.meteor/heroku_build/bin/node $MRT_APP_DIR/.meteor/heroku_build/app/main.js
EOF
